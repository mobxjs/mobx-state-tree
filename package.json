{
	"devDependencies": {
		"lerna": "^2.2.0",
		"husky": "^0.13.4"
	},
	"scripts": {
		"clean": "lerna clean",
		"build": "lerna run build",
		"test": "lerna run test",
		"precommit": "lerna run precommit"
	}
}
