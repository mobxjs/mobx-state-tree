{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "debug unit test",
            "program": "${workspaceRoot}/node_modules/.bin/jest",
            "args": ["-i", "${file}"]
        }
    ]
}
